name: gmail_client
description: "Core Gmail/Google Workspace client — send and receive emails via Supabase Edge Functions. Pure Dart, framework-agnostic."
version: 1.0.0
repository: https://github.com/your-org/enviar_gmail
issue_tracker: https://github.com/your-org/enviar_gmail/issues
topics:
  - gmail
  - email
  - supabase
  - google-workspace

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  supabase: ">=2.0.0 <3.0.0"

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
